<div class="col-md-6">
  <div class="card">
    <div class="header">
      <h4 class="title"><%= @campaign.name %></h4>
      <p class="category">All info about the campaign</p>
      <%= link_to 'Edit', edit_campaign_path(@campaign) %>
    </div>
    <div class="content">
      <%= @campaign.inspect %>
    </div>
  </div>
</div>

<%= render "layouts/dashboard/segments", campaign: @campaign %>